using System;

namespace CShp_FuncLocal
{
    public class Calculo
    {
        public static long Calcular(float x)
        {
            float xaux = x;
            while (Math.pow(x+5,2) > Math.pow(xaux + 5, 2))
            {
                Multiplicar(numero - 1);
                numero--;
            }

            void Multiplicar(int x)
            {
                fatorial *= x;
            }
            return fatorial;
        }
    }
}
